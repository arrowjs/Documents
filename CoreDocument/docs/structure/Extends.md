Giúp mở rộng các tính năng cho features

Ví dụ với mỗi feature bạn muốn thêm các thuộc tính vào hàm riêng bạn sử dụng extends. 
```
   extend :{
                system :true,
                active: true
                getLayout : function() {
                
                }
        },
```


